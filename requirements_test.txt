# This pin is used for local development and direct `pip install -r` runs.
# CI runs a matrix across two pins (minimum HA and current); see
# .github/workflows/pytest.yml for details.
pytest-homeassistant-custom-component==0.13.356

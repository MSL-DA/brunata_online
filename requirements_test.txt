# This pins the Home Assistant version CI tests against — the version you
# would write in a support request, not the test package's own version
# number. pytest-homeassistant-custom-component is left unpinned below: it
# only exists to expose HA's test fixtures, so pip picks whichever release of
# it matches the homeassistant version pinned here.
#
# It is not an ordinary dependency, and dependabot.yml is set up to bump it
# with the Home Assistant version visible in the pull request title, so the
# number below moves on its own schedule. Nothing else should restate it: a
# document or docstring naming this version goes stale at the next bump with
# nothing to catch it. Point at this file instead. The lint toolchain is
# pinned separately in requirements_lint.txt precisely because it *is*
# ordinary — do not merge the two files.
homeassistant==2026.9.0
pytest-homeassistant-custom-component
